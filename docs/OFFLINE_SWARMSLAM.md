# Offline Swarm-SLAM

The offline Swarm-SLAM integration has been moved into the standalone
`offline_swarmslam/` folder so it can be moved out of this robot-side repo.

Use:

```bash
cd offline_swarmslam
```

Then follow `offline_swarmslam/README.md`.
